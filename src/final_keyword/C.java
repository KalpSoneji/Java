package final_keyword;

public class C extends B{
		
		@Override
		void test1()
		{
			
		}
}

// B can't have a child if its final.
//Neither can its function be overridden in its child class if not final 